# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository Status

This repository is currently empty — it contains only a placeholder `README.md` (a single `# claude` heading) and no source code, build configuration, dependency manifests, tests, or tooling. There is no language, framework, or stack established yet.

When the user asks for work here, expect to be bootstrapping the project. Ask the user what stack, language, or framework they want before scaffolding files, rather than guessing. Once code is added, update this file with build/test/run commands and a real architecture overview.

## Branching

Per the working instructions for this session, develop on `claude/add-claude-documentation-apX0e` and push to that branch. The default integration branch is `main`.
